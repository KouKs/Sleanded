<section class="part dark-grey">
    <div class="area">
        <h2 class="left white-text">Dashboard</h2>
    </div>    
</section>
<section class="part">
    <div class="half">   
        <h3 class="center dark-grey-text">New messages</h3>
        <table class="admin">
            <tr>
                <th>Name</th>
                <th>E-mail</th>
                <th>Text</th>
            </tr>
            <?php foreach( $this->messages as $message ) : ?>
                <tr>
                    <td><?php echo $message->name; ?></td>
                    <td><?php echo $message->email; ?></td>
                    <td><?php echo strlen( $message->text ) > 30 ? substr( $message->text , 0 , 30 ) . "..." : $message->text; ?></td>
                </tr>
            <?php endforeach; ?>
        </table>
    </div>    
    <div class="half">
        <h3 class="center dark-grey-text">Pending TODOs</h3>
    </div>     
    <div class="half">
        <h3 class="center dark-grey-text">Latest blog posts</h3>
    </div>  
</section>